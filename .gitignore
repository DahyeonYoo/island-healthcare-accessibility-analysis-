# 이 폴더에는 대용량 데이터(500M 데이터셋, xls/csv 등)가 많아
# 기본적으로 모든 것을 무시하고, 코드(scripts)만 버전 관리합니다.

# 기본적으로 최상위의 모든 항목 무시
/*

# 버전 관리 대상은 아래만 포함
!.gitignore
!scripts/

# scripts 내부의 데이터/캐시성 파일은 제외
scripts/**/__pycache__/
scripts/**/*.pyc
scripts/**/.Rhistory
scripts/**/.RData
